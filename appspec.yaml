version: 1
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "microservice-task"
        LoadBalancerInfo:
          ContainerName: "microservice"
          ContainerPort: 5000
